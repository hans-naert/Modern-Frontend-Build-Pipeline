/// <reference types="vite/client" />
// (Optional) Additional global augmentations can go here. The service worker file
// now handles its own typing via a local cast, avoiding global redeclarations.